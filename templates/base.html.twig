<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Portfolio — Evan Chavot{% endblock %}</title>

    {% block stylesheets %}
        <style>
            /* --- Boutons de navigation en bas --- */
            .bottom-nav {
                width: 100%;
                padding: 28px 0;
                display: flex;
                justify-content: center;
                gap: 32px;
                flex-wrap: wrap;
                margin-top: 50px;
            }

            .nav-btn {
                background: #075b82;
                color: white;
                padding: 20px 42px;
                border-radius: 18px;
                font-size: 18px;
                text-decoration: none;
                display: flex;
                align-items: center;
                justify-content: center;
                font-weight: 500;
                min-width: 240px;
                text-align: center;
                transition: 0.2s;
            }

            .nav-btn:hover {
                background: #0a6f9c;
                transform: translateY(-4px);
            }

            .nav-btn.current {
                background: #0a6f9c;
                cursor: default;
            }
        </style>

        {% block page_styles %}{% endblock %}
    {% endblock %}
</head>

<body>

    {% block body %}{% endblock %}

    <footer>
        <div class="bottom-nav">
            <a href="{{ path('home') }}" class="nav-btn">← Accueil</a>

            <div class="nav-btn current">
                Vous êtes ici :<br> {{ block('current_page_label') }}
            </div>

            <a href="#" class="nav-btn">→ En savoir plus</a>
        </div>
    </footer>

</body>
</html>
